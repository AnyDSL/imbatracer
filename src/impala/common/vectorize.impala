fn vrange4(a: int, b: int, body: fn(int, fn())) -> () {
    for i in vectorize(4, a, b) {
       body(i);
    }
}

fn vrange8(a: int, b: int, body: fn(int, fn())) -> () {
    for i in vectorize(8, a, b) {
       body(i);
    }
}
