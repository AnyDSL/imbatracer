fn sample_hemilight(hemi: HemiLight) -> (Vec3, Vec3, f32) {
    // TODO : Sample an hemispherical light
    
    let pos;
    let dir;

    (pos, dir, invoke_lightshader(light.shader, light.intensity, pos, dir))
}

fn sample_arealight(area: AreaLight) -> (Vec3, Vec3, f32) {
    // TODO : Sample an area light
    
    let pos;
    let dir;

    (pos, dir, invoke_lightshader(light.shader, light.intensity, pos, dir))
}

