
fn get_buffer(buffer_id : BufferRef) -> Buffer {
    // TODO implement buffer management
    let mut tmpdat = [1.0f, 1.0f, 1.0f, 1.0f];
    let ptmp = &tmpdat;
    (Buffer { format : BUFFER_F32, data : ptmp as &[u8], size : 4 * 4 })
}
